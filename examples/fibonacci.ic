function fib(n) {
    if (n <= 1) {
        return n;
    }
    
    fib(n-1) + fib(n-2)
}

print("Enter the length of the sequence:");
set n = readline();

for i in 0 to parse_number(n) {
    print(fib(i));
}